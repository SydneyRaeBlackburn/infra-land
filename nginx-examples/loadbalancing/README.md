# Nginx Load Balancing Examples

## Build & Run
```sh
# update build path for Nginx in docker-compose.yml to the algorithm you want to run
$ docker-compose up -d --build

# Run load test
$ ./test-load.sh
```